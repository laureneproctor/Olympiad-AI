# Defines final inference strategy
# - Model checkpoint to use
# - How many samples per problem
# - Generation settings
# - Output predictions file path (As kaggle submission???)
run:
  seed: 42

paths:
  data_config_path: null  # uses default if null
  model_checkpoint: "/path/to/your/model"
  dataset_path: "/path/to/problem/dataset"
  output_path: "runs/preds.jsonl"

prompting:
  system_prompt: |
    You are a mathematical problem solver.
    Solve step-by-step.
    At the end, output a line exactly of the form:
    FINAL_ANSWER: <integer>
    Where <integer> is an integer between 0 and 99999.

generation:
  n_samples: 8
  max_new_tokens: 512
  temperature: 0.7
  top_p: 0.95